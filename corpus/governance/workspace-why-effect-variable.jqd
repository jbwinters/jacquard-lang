(defterm
  ((binding workspace-why-effect-variable ()
    (lam ((pvar policy) (pvar simulators))
      (app (var workspace.live)
        (var policy) (var simulators)
        (lam ()
          (let nonrec (pvar callable)
            (lam ()
              (app (var workspace.read-file)
                (app (var path-value) (lit "README.md"))))
            (app (var callable)))))))))
