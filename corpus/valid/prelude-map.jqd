; list.map (add 1) over [1, 2, 3] with uncurried add wrapped in a lambda (SL.3 grid)
(app (var list.map)
  (app (var cons) (lit 1)
    (app (var cons) (lit 2)
      (app (var cons) (lit 3) (var nil))))
  (lam ((pvar x)) (app (var add) (var x) (lit 1))))
