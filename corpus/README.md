# Corpus

The conformance corpus will be filled in during W1.2 and W1.7.

- `valid/` contains `.wft` files that should parse, validate, resolve, and hash.
- `invalid/` contains `.wft` files that should fail at a named stage.
- `golden/` contains expected printer, hash, diagnostic, and CLI output as those checks land.

