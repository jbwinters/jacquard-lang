# Tooling

Semantic program repair, Warp-backed demo checks, and native/console examples.
The repair demo requires an explicit eval grant and prints the resulting
semantic code diff:

```sh
sh demos/tooling/repair.sh
```
