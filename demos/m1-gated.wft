; M1 demo 3: gated eval. Runs only under --allow eval; otherwise the
; unhandled effect kills the program (exit 3) — capability security at work.
(app (var eval-code) (quote (app (var mul) (lit 6) (lit 7))))
