name: Release Evidence

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch, tag, or commit to reproduce"
        required: false
        default: "release/0.1-evidence"
      base:
        description: "Candidate base commit that must be an ancestor"
        required: false
        default: "aec2c63"
  push:
    branches:
      - "release/**"
    tags:
      # weft-core-* is the pre-rename tag family (history); new tags use
      # jacquard-core-*.
      - "weft-core-*"
      - "jacquard-core-*"

permissions:
  contents: read

concurrency:
  group: release-evidence-${{ github.ref }}
  cancel-in-progress: false

# This workflow reproduces the frozen 0.1 evidence pack, whose tree predates
# the Weft -> Jacquard rename; the WEFT_* names below feed that old tree and
# are deliberate. Env vars are passed in both spellings so old-tag checkouts
# of the reproduce script keep working.
env:
  OCAML_COMPILER: 5.1.1
  WEFT_PRELUDE: ${{ github.workspace }}/prelude
  JACQUARD_PRELUDE: ${{ github.workspace }}/prelude

jobs:
  reproduce:
    name: Reproduce 0.1 evidence
    runs-on: ubuntu-latest

    steps:
      - name: Checkout candidate
        uses: actions/checkout@v7.0.0
        with:
          fetch-depth: 0
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Set up OCaml
        uses: ocaml/setup-ocaml@v3.6.1
        with:
          ocaml-compiler: ${{ env.OCAML_COMPILER }}

      - name: Reproduce release evidence
        run: |
          WEFT_RELEASE_REF=HEAD \
          WEFT_RELEASE_BASE="${{ github.event.inputs.base || 'aec2c63' }}" \
          JACQUARD_RELEASE_REF=HEAD \
          JACQUARD_RELEASE_BASE="${{ github.event.inputs.base || 'aec2c63' }}" \
          scripts/release/reproduce-0.1.sh

      - name: Upload evidence transcripts
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: weft-release-0.1-evidence-${{ github.run_number }}
          if-no-files-found: error
          path: |
            docs/release/0.1/
            logs/release/0.1/
